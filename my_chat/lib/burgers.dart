import 'package:flutter/material.dart';
import 'dart:convert';

class Burgers {
  String name;
  String description;
  String price;
  bool bestSale;
  Burgers({this.name, this.description, this.price, this.bestSale});
}
